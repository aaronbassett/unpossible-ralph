# Rust Formatting Configuration
# Per constitution principle I (Unix Philosophy) and project conventions
# Using only stable rustfmt options

edition = "2021"
max_width = 100
tab_spaces = 4
use_small_heuristics = "Default"
