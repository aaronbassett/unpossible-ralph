# Phase 3 Retrospective: TOML Configuration [US1]

## What Worked Well
- serde with deny_unknown_fields handles E29 automatically
- thiserror provides clean error type derivation
- Untagged enum for AgentConfig handles both simple and extended formats cleanly

## What Didn't Work
- Initial test fixture missing {{prompt}} in agent commands - validation caught it

## Workarounds & Solutions
- Validation rules check for required placeholders in both prompts and agent commands
- Human-readable error messages include field names per constitution principle II

## Packages & Dependencies
- toml 0.8 works well with serde
- thiserror provides clean Display implementations

## Patterns & Code
- Config validation as separate step after parsing (parse then validate)
- AgentConfig accessor methods (command(), timeout()) for uniform access
- Contains_placeholder helper for DRY validation

## For Next Time
- Test fixtures should be validated against the same rules as production configs
