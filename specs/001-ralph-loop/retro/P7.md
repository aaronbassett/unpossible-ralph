# Phase 7 Retrospective: Next-Action Prompt Generation [US5]

## What Worked Well
- Template selection logic (starting vs continuation) already implemented in Phase 5
- run_next_action_agent() handles all output variables (dev_response, dev_errors, review_response, review_errors)
- next_iteration() method on LoopState handles state transition cleanly

## What Didn't Work
<!-- Problems, issues, limitations encountered -->

## Workarounds & Solutions
- FR38/FR39: get_dev_prompt_template() checks is_first_iteration() to select template
- FR37: next_prompt stored in LoopState after next-action agent completes
- FR41: Fatal error handling already in run_next_action_agent()

## Packages & Dependencies
- No new dependencies - all functionality built on existing modules

## Patterns & Code
- Template selection: is_first_iteration() → starting/continuation
- State transition: next_iteration(next_prompt) increments iteration, resets retry, stores prompt
- Context building includes all required variables for each prompt type

## For Next Time
- Consider adding async tests for agent execution flow
